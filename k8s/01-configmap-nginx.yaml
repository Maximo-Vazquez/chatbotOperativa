apiVersion: v1
kind: ConfigMap
metadata:
  name: chatbot-operativa-nginx-conf
  namespace: default
data:
  default.conf: |
    server {
        listen 80;
        server_name ia.indutienda.com;
        client_max_body_size 10m;

        # Sirve archivos estaticos desde el volumen compartido
        location /static/ {
            alias /var/www/static/;
            expires 30d;
            add_header Cache-Control "public, max-age=2592000";
        }

        # SSE: no acumular fragmentos del modelo antes de enviarlos al navegador.
        location /chat/api/stream/ {
            proxy_pass http://127.0.0.1:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 130s;
        }

        # Proxy hacia Django en el mismo Pod
        location / {
            proxy_pass http://127.0.0.1:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
        }
    }
