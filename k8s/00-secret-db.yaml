apiVersion: v1
kind: Secret
metadata:
  name: db-credentials-chatbot
  namespace: default
type: Opaque
stringData:
  # Plantilla de referencia.
  # No guardar secretos reales en este repositorio.
  host: "<DB_HOST>"
  port: "<DB_PORT>"
  user: "<DB_USER>"
  password: "<DB_PASSWORD>"
