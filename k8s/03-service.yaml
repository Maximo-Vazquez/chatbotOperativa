apiVersion: v1
kind: Service
metadata:
  name: chatbot-operativa-service
  namespace: default
spec:
  selector:
    app: chatbot-operativa
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
